# Postgres (one instance, two DBs: app + airflow)
POSTGRES_USER=researchai
POSTGRES_PASSWORD=researchai
APP_DB=researchai_app
AIRFLOW_DB=researchai_airflow

# OpenSearch
OPENSEARCH_PORT=9200
OPENSEARCH_HOST=opensearch
DISABLE_SECURITY_PLUGIN=true

# MinIO
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minio123
MINIO_CONSOLE_PORT=9001
MINIO_API_PORT=9000
MINIO_BUCKET=researchai

# API / UI
API_PORT=8000
UI_PORT=7860

# Airflow
AIRFLOW__CORE__EXECUTOR=LocalExecutor
AIRFLOW__WEBSERVER__RBAC=True
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PWD=admin
AIRFLOW_ADMIN_EMAIL=admin@example.com

# Ollama
OLLAMA_PORT=11434

# GCP Production Configuration (for scripts)
# Cloud SQL
GCP_POSTGRES_HOST=your_cloud_sql_ip
GCP_POSTGRES_USER=postgres
GCP_POSTGRES_PASSWORD=your_secure_password
GCP_POSTGRES_DB=researchai
GCP_POSTGRES_PORT=5432

# GCP OpenSearch
GCP_OPENSEARCH_HOST=your_opensearch_ip
GCP_OPENSEARCH_PORT=9200
